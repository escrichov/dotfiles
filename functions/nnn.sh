#!/usr/bin/env bash

# nnn: gestor de archivos de terminal. '-P p' lanza al abrir el plugin mapeado
# a la tecla 'p', que en env.sh es NNN_PLUG='p:preview-tui' (panel de vista
# previa del archivo seleccionado; va mejor dentro de tmux).
alias nnn='nnn -P p'
