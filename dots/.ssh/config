# SSH Configuration
Host *
   IdentitiesOnly yes # Only try identities defined in /etc/ssh/ssh_config. No try all keys in ~/.ssh/

TCPKeepAlive=yes
ServerAliveInterval=15
ServerAliveCountMax=6
Compression=yes
ControlMaster auto
ControlPath /tmp/%r@%h:%p
ControlPersist yes
ConnectTimeout=5
